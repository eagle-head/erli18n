#!/usr/bin/env bash
# Pre-push hook — full quality gate including dialyzer, eqwalizer, tests.
# Installed via `hooks/install.sh` (sets git core.hooksPath to this dir).
exec "$(git rev-parse --show-toplevel)/bin/quality-gate.sh" --pre-push
